<li>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t('user.delete'), user, method: :delete,  data: { confirm: t('delete.confirmation') } %>
  <% end %>
</li>
