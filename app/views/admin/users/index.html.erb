<% provide(:title, 'All users') %>
<h1><%= t('user.h1_alluser') %></h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render @users %>
  <%  end %>
</ul>

<%= will_paginate %>
